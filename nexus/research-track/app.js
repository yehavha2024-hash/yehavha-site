(()=>{'use strict';const data=window.RESEARCH_TRACK||{};const track=document.getElementById('track');const knowledge=document.getElementById('knowledgeGrid');const updatedAt=document.getElementById('updatedAt');function make(tag,className,text){const el=document.createElement(tag);if(className)el.className=className;if(text!==undefined)el.textContent=text;return el}function renderStage(stage){const article=make('article',`stage-card tone-${stage.tone||'future'}`);article.id=stage.id;const rail=make('div','stage-rail');rail.append(make('span','stage-order',stage.order));const body=make('div','stage-body');const top=make('div','stage-top');const titleWrap=make('div','stage-title-wrap');titleWrap.append(make('p','eyebrow',stage.eyebrow),make('h2','',stage.title));top.append(titleWrap,make('span','stage-status',stage.status));body.append(top,make('p','stage-summary',stage.summary));if(Array.isArray(stage.items)&&stage.items.length){const items=make('div','stage-items');stage.items.forEach((item)=>{const box=make('div','stage-item');box.append(make('span','item-type',item.type),make('strong','',item.title));if(item.note)box.append(make('p','',item.note));if(item.url){const link=make('a','item-link','원문 보기 →');link.href=item.url;link.target='_blank';link.rel='noopener noreferrer';box.append(link)}items.append(box)});body.append(items)}else{body.append(make('p','empty-note','성과가 확정되면 이 단계에 순차적으로 추가합니다.'))}article.append(rail,body);return article}function renderKnowledge(item){const article=make('article','knowledge-card');article.append(make('h3','',item.title),make('p','',item.description));const link=make('a','','연구 기반 보기 →');link.href=item.url;link.target='_blank';link.rel='noopener noreferrer';article.append(link);return article}(data.stages||[]).forEach((stage)=>track?.append(renderStage(stage)));(data.knowledge||[]).forEach((item)=>knowledge?.append(renderKnowledge(item)));if(updatedAt&&data.updatedAt)updatedAt.textContent=`업데이트 ${String(data.updatedAt).replaceAll('-','.')}`})();
