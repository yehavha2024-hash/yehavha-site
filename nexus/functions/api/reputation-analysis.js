const TARGET_TYPES = Object.freeze({
  person:{label:'인물'}, organization:{label:'회사·기관'}, product:{label:'상품'}, service:{label:'서비스'}, place:{label:'장소'}
});
const NON_REPUTATION_GROUPS = new Set(['기본정보','공식·언론']);
const POSITIVE = ['좋다','좋은','만족','추천','친절','우수','편리','안정','성장','개선','합리','긍정','장점','깨끗','신뢰'];
const NEGATIVE = ['나쁘','불만','불친절','느리','지연','비싸','불편','문제','오류','고장','과도','야근','수직','퇴사','환불','논란','비판','단점'];
const HIGH_RISK = ['사기꾼','범죄자','횡령','배임','성범죄','불법업체','마약','폭행','살인','탈세'];
const TOPICS = Object.freeze({
  organization:{'업무강도':['업무량','야근','업무강도','과중','워라밸','근무시간'],'조직문화':['조직문화','수직적','수평적','소통','분위기','경영진'],'보상·복지':['급여','연봉','복지','성과급','보상'],'채용·면접':['면접','채용','입사','전형'],'퇴사·이직':['퇴사','이직','퇴직','중도퇴사'],'거래·신뢰':['납기','정산','거래','계약','신뢰','대금']},
  product:{'품질':['품질','마감','내구성','불량','고장'],'가격·가치':['가격','가성비','비싸','저렴'],'배송':['배송','도착','지연','포장'],'사용성':['사용','편리','불편','성능'],'사후지원':['AS','A/S','환불','교환','고객센터']},
  service:{'서비스 품질':['품질','서비스','처리','안정성'],'고객응대':['고객센터','응대','친절','상담'],'가격·비용':['가격','요금','비용','수수료'],'환불·해지':['환불','해지','취소','위약금'],'사용성':['사용','편리','불편','오류','장애']},
  place:{'친절·응대':['친절','불친절','응대','직원'],'가격':['가격','비싸','저렴','가성비'],'청결·시설':['청결','위생','시설','깨끗'],'대기·예약':['대기','예약','시간','줄'],'재방문':['재방문','다시','추천','재이용']},
  person:{'공개경력':['경력','재직','학력','활동','수상'],'전문성 평가':['전문성','실력','성과','능력'],'공적 활동':['발언','인터뷰','강연','저서','프로젝트'],'논쟁·상반평가':['논란','비판','반박','평가']}
});

function json(body,status=200){return new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store, max-age=0','x-content-type-options':'nosniff'}})}
function clean(value,max=160){return String(value||'').replace(/\s+/g,' ').trim().slice(0,max)}
function decodeHtml(value){return String(value||'').replace(/<!\[CDATA\[([\s\S]*?)\]\]>/g,'$1').replace(/&#(\d+);/g,(_,n)=>String.fromCharCode(Number(n))).replace(/&#x([0-9a-f]+);/gi,(_,n)=>String.fromCharCode(parseInt(n,16))).replace(/&amp;/g,'&').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&quot;/g,'"').replace(/&#39;|&apos;/g,"'").replace(/&nbsp;/g,' ')}
function stripTags(value){return decodeHtml(value).replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim()}
function entityText(value){return String(value||'').normalize('NFKC').toLowerCase().replace(/&[a-z0-9#]+;/gi,' ').replace(/[\s"'`’‘“”·ㆍ•:;,.!?()[\]{}<>/\\|_+=~^*-]+/g,'')}
function sourceHost(url){try{return new URL(url).hostname.replace(/^www\./,'')}catch{return ''}}
function normalizeUrl(url){try{const u=new URL(url);['utm_source','utm_medium','utm_campaign','utm_term','utm_content','gclid','fbclid'].forEach(k=>u.searchParams.delete(k));u.hash='';return u.toString()}catch{return url}}
function tag(block,name){const m=String(block||'').match(new RegExp(`<${name}(?:\\s[^>]*)?>([\\s\\S]*?)<\\/${name}>`,'i'));return m?stripTags(m[1]):''}
function targetAliases(type,target){const raw=String(target||'').normalize('NFKC').trim();const values=new Set([raw]);if(type==='organization'){values.add(raw.replace(/\(주\)|㈜|주식회사|유한회사|재단법인|사단법인/gi,'').trim());values.add(raw.replace(/\b(co\.?|corp\.?|corporation|inc\.?|ltd\.?|llc)\b/gi,'').trim())}return [...values].map(entityText).filter(v=>v.length>=2)}
function relevantToTarget(type,target,item){let decoded=item.url||'';try{decoded=decodeURIComponent(decoded)}catch{}const hay=entityText(`${item.title} ${item.snippet} ${item.sourceName||''} ${decoded}`);const aliases=targetAliases(type,target);if(aliases.some(a=>hay.includes(a)))return true;const tokens=String(target||'').normalize('NFKC').toLowerCase().split(/[\s"'`’‘“”·ㆍ•:;,.!?()[\]{}<>/\\|_+=~^*-]+/).map(entityText).filter(t=>/[가-힣]/.test(t)?t.length>=2:t.length>=3);return tokens.length>=2&&tokens.every(t=>hay.includes(t))}
function dedupe(items){const seen=new Set(),out=[];for(const item of items){const url=normalizeUrl(item.url);const key=`${url}|${entityText(item.title).slice(0,80)}`;if(!url||seen.has(key))continue;seen.add(key);out.push({...item,url})}return out}
function diversify(items,perHostGroup=4,max=32){const counts=new Map(),out=[];for(const item of items){const host=sourceHost(item.url)||item.sourceName||item.provider||'unknown';const key=`${item.group}|${host}`;const count=counts.get(key)||0;if(count>=perHostGroup)continue;counts.set(key,count+1);out.push(item);if(out.length>=max)break}return out}

async function fetchText(url,timeoutMs=6500){const c=new AbortController();const timer=setTimeout(()=>c.abort(),timeoutMs);try{const r=await fetch(url,{headers:{accept:'text/html,application/xhtml+xml,application/rss+xml,application/xml;q=0.9,*/*;q=0.7','user-agent':'Mozilla/5.0 (compatible; YEHAVHA-NEXUS-Reputation/2.0; +https://yehavha.com/)'},signal:c.signal});if(!r.ok)throw new Error(`upstream_${r.status}`);return await r.text()}finally{clearTimeout(timer)}}
function bingRssUrl(q){return `https://www.bing.com/search?format=rss&setlang=ko-KR&cc=KR&q=${encodeURIComponent(q)}`}
function bingHtmlUrl(q){return `https://www.bing.com/search?setlang=ko-KR&cc=KR&count=12&q=${encodeURIComponent(q)}`}
function ddgHtmlUrl(q){return `https://html.duckduckgo.com/html/?kl=kr-kr&q=${encodeURIComponent(q)}`}
function googleNewsUrl(q){return `https://news.google.com/rss/search?hl=ko&gl=KR&ceid=KR:ko&q=${encodeURIComponent(q)}`}

function parseRss(xml,provider,group){const items=[];const blocks=String(xml||'').match(/<item\b[\s\S]*?<\/item>/gi)||[];for(const block of blocks.slice(0,12)){const title=tag(block,'title'),link=tag(block,'link'),description=tag(block,'description'),pubDate=tag(block,'pubDate'),sourceName=tag(block,'source');if(title&&link)items.push({title,url:link,snippet:description,publishedAt:pubDate||null,provider,group,sourceName})}return items}
function parseBingHtml(html,group){const items=[];const blocks=String(html||'').match(/<li[^>]+class="[^"]*b_algo[^"]*"[\s\S]*?<\/li>/gi)||[];for(const block of blocks.slice(0,12)){const a=block.match(/<h2[^>]*>\s*<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/i)||block.match(/<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/i);if(!a)continue;const p=block.match(/<p[^>]*>([\s\S]*?)<\/p>/i);const url=decodeHtml(a[1]);if(/^https?:\/\//i.test(url))items.push({title:stripTags(a[2]),url,snippet:p?stripTags(p[1]):'',publishedAt:null,provider:'Bing Web',group,sourceName:''})}return items}
function decodeDdgUrl(href){const raw=decodeHtml(href);try{const u=new URL(raw,'https://html.duckduckgo.com');const uddg=u.searchParams.get('uddg');return uddg?decodeURIComponent(uddg):u.href}catch{return raw}}
function parseDdgHtml(html,group){const items=[];const blocks=String(html||'').match(/<div[^>]+class="[^"]*result[^"]*"[\s\S]*?(?=<div[^>]+class="[^"]*result[^"]*"|$)/gi)||[];for(const block of blocks.slice(0,12)){const a=block.match(/<a[^>]+class="[^"]*result__a[^"]*"[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/i)||block.match(/<a[^>]+href="([^"]+)"[^>]+class="[^"]*result__a[^"]*"[^>]*>([\s\S]*?)<\/a>/i);if(!a)continue;const s=block.match(/<(?:a|div)[^>]+class="[^"]*result__snippet[^"]*"[^>]*>([\s\S]*?)<\/(?:a|div)>/i);const url=decodeDdgUrl(a[1]);if(/^https?:\/\//i.test(url))items.push({title:stripTags(a[2]),url,snippet:s?stripTags(s[1]):'',publishedAt:null,provider:'DuckDuckGo',group,sourceName:''})}return items}

function searchPlan(type,target){const q=`"${target}"`;if(type==='organization')return [
  {group:'기본정보',queries:[q,`${q} 기업정보 회사소개`,`${q} 공식 홈페이지`]},
  {group:'기본정보',queries:[`site:jobkorea.co.kr ${q}`,`site:saramin.co.kr ${q}`]},
  {group:'재직·면접',queries:[`site:jobplanet.co.kr ${q}`,`site:saramin.co.kr ${q} 기업리뷰`,`site:jobkorea.co.kr ${q} 면접 연봉`]},
  {group:'공개평가',queries:[`${q} 후기 평판 리뷰`,`${q} 근무환경 조직문화`]},
  {group:'공식·언론',queries:[`${q} 보도자료 뉴스`,`${q} 대표 사업`],news:true}
];
  if(type==='person')return [{group:'기본정보',queries:[q,`${q} 프로필 경력 소속`]},{group:'공개평가',queries:[`${q} 평가 평판`]},{group:'공식·언론',queries:[`${q} 인터뷰 보도`],news:true}];
  if(type==='product')return [{group:'기본정보',queries:[q,`${q} 제품소개 제조사`]},{group:'구매·사용',queries:[`${q} 구매후기 리뷰 사용기`]},{group:'문제·지원',queries:[`${q} 불량 환불 AS`]},{group:'공식·언론',queries:[`${q} 출시 리콜 뉴스`],news:true}];
  if(type==='service')return [{group:'기본정보',queries:[q,`${q} 서비스소개 운영사`]},{group:'이용경험',queries:[`${q} 이용후기 리뷰`]},{group:'문제·지원',queries:[`${q} 환불 해지 고객센터`]},{group:'공식·언론',queries:[`${q} 공지 뉴스`],news:true}];
  return [{group:'기본정보',queries:[q,`${q} 주소 운영시간`]},{group:'방문경험',queries:[`${q} 방문후기 리뷰`]},{group:'이용평가',queries:[`${q} 친절 가격 청결 대기`]},{group:'공식·언론',queries:[`${q} 공지 뉴스`],news:true}]
}
async function runQuery(query,group,news=false){const jobs=[
  fetchText(bingRssUrl(query)).then(x=>parseRss(x,'Bing RSS',group)),
  fetchText(bingHtmlUrl(query)).then(x=>parseBingHtml(x,group)),
  fetchText(ddgHtmlUrl(query)).then(x=>parseDdgHtml(x,group))
];if(news)jobs.push(fetchText(googleNewsUrl(query)).then(x=>parseRss(x,'Google News',group)));const settled=await Promise.allSettled(jobs);return {items:settled.flatMap(r=>r.status==='fulfilled'?r.value:[]),successes:settled.filter(r=>r.status==='fulfilled').length,failures:settled.filter(r=>r.status==='rejected').length}}
async function collect(type,target){const plan=searchPlan(type,target);const jobs=[];for(const bucket of plan)for(const query of bucket.queries)jobs.push(runQuery(query,bucket.group,bucket.news));const results=await Promise.all(jobs);const raw=dedupe(results.flatMap(r=>r.items));const relevant=raw.filter(item=>relevantToTarget(type,target,item));return {items:diversify(relevant,4,32),rawCount:raw.length,irrelevantCount:raw.length-relevant.length,providerSuccesses:results.reduce((n,r)=>n+r.successes,0),providerFailures:results.reduce((n,r)=>n+r.failures,0)}}

function scoreSentiment(text){const t=String(text||'').toLowerCase();let pos=0,neg=0;for(const k of POSITIVE)if(t.includes(k.toLowerCase()))pos++;for(const k of NEGATIVE)if(t.includes(k.toLowerCase()))neg++;return pos>neg?'positive':neg>pos?'negative':'neutral'}
function highRisk(text){const t=String(text||'').toLowerCase();return HIGH_RISK.some(k=>t.includes(k.toLowerCase()))}
function topicMatches(type,text){const t=String(text||'').toLowerCase();return Object.entries(TOPICS[type]||{}).filter(([,words])=>words.some(w=>t.includes(w.toLowerCase()))).map(([name])=>name)}
function annotateEvidence(type,items){return items.map((item,index)=>{const text=`${item.title} ${item.snippet}`;return {id:`S${String(index+1).padStart(2,'0')}`,...item,host:sourceHost(item.url)||item.sourceName||item.provider,topics:topicMatches(type,text),sentiment:scoreSentiment(text),highRisk:highRisk(text)}})}
function buildSignals(type,evidence){const m=new Map();for(const e of evidence){if(e.highRisk)continue;for(const topic of e.topics){if(!m.has(topic))m.set(topic,{topic,positive:[],negative:[],neutral:[],hosts:new Set(),positiveHosts:new Set(),negativeHosts:new Set(),neutralHosts:new Set()});const r=m.get(topic);r[e.sentiment].push(e.id);if(e.host){r.hosts.add(e.host);r[`${e.sentiment}Hosts`].add(e.host)}}}return [...m.values()].map(r=>({topic:r.topic,positive:r.positive,negative:r.negative,neutral:r.neutral,independentSources:r.hosts.size,positiveIndependentSources:r.positiveHosts.size,negativeIndependentSources:r.negativeHosts.size,neutralIndependentSources:r.neutralHosts.size,total:r.positive.length+r.negative.length+r.neutral.length,state:r.positive.length&&r.negative.length?'conflicted':r.negative.length>r.positive.length?'negative':r.positive.length>r.negative.length?'positive':'mixed'})).sort((a,b)=>b.independentSources-a.independentSources||b.total-a.total).slice(0,8)}
function isStrongSignal(s){if(s.state==='negative')return s.negativeIndependentSources>=2;if(s.state==='positive')return s.positiveIndependentSources>=2;if(s.state==='conflicted')return s.positiveIndependentSources>=1&&s.negativeIndependentSources>=1&&s.independentSources>=2;return s.neutralIndependentSources>=2}
function buildOverview(type,target,profileEvidence){if(!profileEvidence.length)return '';const preferred=[...profileEvidence].sort((a,b)=>{const ah=/^(?:[^.]+\.)?lgrc\.co\.kr$/.test(a.host||'')?2:['jobkorea.co.kr','saramin.co.kr'].some(h=>(a.host||'').endsWith(h))?1:0;const bh=/^(?:[^.]+\.)?lgrc\.co\.kr$/.test(b.host||'')?2:['jobkorea.co.kr','saramin.co.kr'].some(h=>(b.host||'').endsWith(h))?1:0;return bh-ah});const source=preferred.find(e=>e.snippet)||preferred[0];const snippet=clean(source.snippet,420);return snippet||`${target}에 관한 기본·공식 공개자료 ${profileEvidence.length}건이 확인되었습니다. 대표 자료는 “${clean(source.title,140)}”입니다.`}
function buildProfile(type,target,evidence,reputationEvidence,signals,collection){const profileEvidence=evidence.filter(e=>NON_REPUTATION_GROUPS.has(e.group));const overview=buildOverview(type,target,profileEvidence);const strong=signals.filter(isStrongSignal);let reputationStatus='none';let reputationMessage=`${target}에 관한 직접 일치 평판·후기 자료가 확인되지 않았습니다. 자료 부재 자체는 긍정·부정 판단 근거가 아닙니다.`;if(reputationEvidence.length&&strong.length===0){reputationStatus='insufficient';reputationMessage=`${target}에 관한 평판·후기 자료 ${reputationEvidence.length}건을 확인했지만 서로 다른 복수 출처에서 같은 방향의 평가가 충분히 반복되지는 않았습니다.`}else if(strong.length){reputationStatus='available';reputationMessage=`${target}에 관해 서로 다른 복수 출처에서 반복되거나 상충하는 평판 신호가 확인되었습니다. 아래 개별 근거를 함께 확인하십시오.`}if(profileEvidence.length)return {status:'identified',overview:overview||`${target}에 관한 기본·공식 공개자료가 확인되었습니다.`,sourceIds:profileEvidence.slice(0,6).map(e=>e.id),sourceCount:profileEvidence.length,reputationStatus,reputationMessage};if(evidence.length)return {status:'limited',overview:`${target}과 직접 일치하는 공개자료는 확인됐지만 기본·공식 소개 자료가 충분하지 않습니다.`,sourceIds:evidence.slice(0,4).map(e=>e.id),sourceCount:0,reputationStatus,reputationMessage};const health=collection.providerSuccesses>0?'검색 공급원은 응답했지만 직접 일치 자료를 확보하지 못했습니다.':'검색 공급원 연결 자체가 충분히 성공하지 못했습니다.';return {status:'unidentified',overview:`${health} 현재 결과를 '인터넷에 자료가 없음'으로 단정하지 않습니다. 회사명 전체·지역·홈페이지 등 식별정보를 함께 입력하면 정확도가 높아집니다.`,sourceIds:[],sourceCount:0,reputationStatus:'none',reputationMessage:'직접 일치가 검증된 평판 자료가 없어 평판 판단을 보류합니다.'}}
function reportSummary(target,signals,profile){const strong=signals.filter(isStrongSignal).slice(0,3);if(!strong.length)return `${profile.overview} ${profile.reputationMessage}`;const parts=strong.map(s=>s.state==='conflicted'?`${s.topic}에서 긍정·부정 평가가 함께 나타납니다`:s.state==='negative'?`${s.topic} 관련 부정적 신호가 복수 독립 출처에서 반복됩니다`:s.state==='positive'?`${s.topic} 관련 긍정적 신호가 복수 독립 출처에서 반복됩니다`:`${s.topic} 관련 언급이 복수 출처에서 반복됩니다`);return `${target}과 직접 연결되는 공개자료만 남겨 교차 확인했습니다. ${parts.join('. ')}. 아래 원문을 함께 확인하십시오.`}
function searchLinks(type,target){const q=encodeURIComponent(target);const links=[{label:'Google 웹검색',url:`https://www.google.com/search?q=${q}`},{label:'네이버 통합검색',url:`https://search.naver.com/search.naver?query=${q}`},{label:'다음 검색',url:`https://search.daum.net/search?q=${q}`}];if(type==='organization')links.push({label:'잡코리아 관련검색',url:`https://www.google.com/search?q=site%3Ajobkorea.co.kr+${q}`},{label:'잡플래닛 관련검색',url:`https://www.google.com/search?q=site%3Ajobplanet.co.kr+${q}`},{label:'사람인 관련검색',url:`https://www.google.com/search?q=site%3Asaramin.co.kr+${q}`});return links}

export async function onRequestPost({request}){let body;try{body=await request.json()}catch{return json({ok:false,error:'invalid_json'},400)}const type=clean(body?.type,24),target=clean(body?.target,120);if(!TARGET_TYPES[type])return json({ok:false,error:'invalid_type'},400);if(target.length<2)return json({ok:false,error:'target_too_short'},400);const collection=await collect(type,target);const evidence=annotateEvidence(type,collection.items);const safe=evidence.filter(e=>!e.highRisk);const held=evidence.filter(e=>e.highRisk).map(e=>({id:e.id,reason:'고위험 주장 키워드가 포함되어 자동 요약에서 제외',url:e.url,title:e.title}));const reputationEvidence=safe.filter(e=>!NON_REPUTATION_GROUPS.has(e.group));const signals=buildSignals(type,reputationEvidence);const profile=buildProfile(type,target,safe,reputationEvidence,signals,collection);return json({ok:true,schema:'nexus-reputation-analysis-v4',target:{type,typeLabel:TARGET_TYPES[type].label,name:target},generatedAt:new Date().toISOString(),methodology:{mode:'public-source-osint',stages:['다중 검색원 탐색','대상 직접일치 확인','기본정보 확인','평판자료 분리','중복 제거','출처 다양화','주제 분류','상반 평가 탐지','고위험 주장 분리','근거 연결'],note:'Bing RSS 한 종류에 의존하지 않고 Bing 웹 결과, DuckDuckGo HTML 결과, 필요한 경우 Google News RSS를 함께 탐색합니다. 회사·기관은 잡코리아·사람인·잡플래닛 등 주요 기업정보·평판 도메인을 별도 검색한 뒤 대상명이 직접 확인되는 결과만 남깁니다. 유료·로그인 제한 원문의 비공개 내용은 수집하지 않습니다.'},profile,executiveSummary:reportSummary(target,signals,profile),metrics:{sources:safe.length,profileSources:profile.sourceCount,reputationSources:reputationEvidence.length,independentHosts:new Set(safe.map(e=>e.host).filter(Boolean)).size,filteredIrrelevant:collection.irrelevantCount,rawCandidates:collection.rawCount,providerSuccesses:collection.providerSuccesses,providerFailures:collection.providerFailures,heldHighRisk:held.length,signals:signals.length},signals,evidence:safe,heldEvidence:held,searchLinks:searchLinks(type,target),disclaimer:'검색 공급원에서 후보가 없거나 직접 일치 결과가 없다는 사실을 대상에 관한 공개자료 부재로 단정하지 않습니다. 자동화된 1차 조사이므로 중요한 결정에는 원문과 공식자료를 직접 확인하십시오.'})}
export async function onRequestGet(){return json({ok:true,service:'NEXUS 평판 분석',version:'4.0',providers:['Bing RSS','Bing Web','DuckDuckGo','Google News'],types:Object.entries(TARGET_TYPES).map(([id,v])=>({id,label:v.label}))})}
