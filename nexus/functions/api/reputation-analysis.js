const TOPICS=Object.freeze({
'업무강도·워라밸':['야근','퇴근','근무시간','업무량','업무강도','워라밸','주말근무','휴가','연차','과중'],
'조직문화·경영진':['조직문화','분위기','대표','경영진','상사','팀장','수직','수평','소통','눈치','압박','괴롭'],
'보상·복지':['급여','연봉','성과급','보상','복지','수당','임금','급여일','체불'],
'채용·면접':['면접','면접관','질문','채용','입사','전형','수습','공석'],
'퇴사·이직·근속':['퇴사','이직','퇴직','근속','전임자','중도퇴사','이직률'],
'업무체계·성장':['업무분장','인수인계','체계','교육','성장','배울','경력','전문성','업무범위']});
const ACTIONS=Object.freeze({
'업무강도·워라밸':'최근 실제 퇴근시간, 주말근무 빈도, 긴급 연락 기준, 결원 시 업무분담, 연차 사용 방식을 구체적으로 확인하십시오.',
'조직문화·경영진':'직속 상사와 보고라인, 대표·관리자의 의사결정 개입 범위, 팀 내 갈등 처리방식, 최근 팀 이직률과 고충 제기 절차를 질문하십시오.',
'보상·복지':'기본급, 수습기간 급여, 급여일, 성과급 기준, 야근·휴일수당, 복지 항목을 오퍼·근로계약서·공식 문서로 확인하십시오.',
'채용·면접':'공석 발생 이유, 전임자 근속기간, 실제 담당업무, 팀 인원, 수습평가 기준과 근로계약 조건을 확인하십시오.',
'퇴사·이직·근속':'같은 직무의 평균 근속기간, 최근 퇴사 추이와 전임자 퇴사 사유를 확인하십시오.',
'업무체계·성장':'업무 인수인계 문서, 교육기간, 담당업무 범위, 의사결정 권한, 평가기준과 경력으로 인정되는 핵심 업무를 확인하십시오.'});
const EXPERIENCE_TERMS=/(재직|전직|퇴사|이직|근무|직원|면접|면접관|상사|대표|경영진|팀장|업무|야근|퇴근|연차|휴가|급여|연봉|복지|수습|조직문화|워라밸|인수인계|업무량|분위기|질문)/i;
const ACTUAL_VOICE=/(많다|많고|많음|적다|적고|적음|좋다|좋고|좋은|나쁘|힘들|어렵|없다|없고|있다|있고|배울|추천|비추천|만족|불만|눈치|자주|거의|친절|불친절|낮다|낮은|높다|높은|잦다|잦은|부족|과도|수직|수평|보수적|자유롭|체계적|압박|장점|단점|면접에서|면접관이|분위기는|업무는|급여는|복지는|대표가|상사가)/i;
const GENERIC_ONLY=/(회사소개|기업소개|기업정보|채용정보|채용공고|공고보기|연봉정보|재무정보|매출액|사업내용|기업개요|인재상|공식 홈페이지|채용중|입사지원)/i;
const REVIEW_URL=/(jobplanet|teamblind|blind|company-review|review|interview|면접후기|기업리뷰|reviews)/i;
const SIGNAL_WORDS=['야근','퇴근','워라밸','업무량','상사','대표','경영진','분위기','수직','수평','소통','복지','급여','연봉','휴가','연차','수습','면접관','질문','퇴사','이직','배울','성장','압박','체계','힘들','좋','나쁘','만족','불만','추천','비추천','장점','단점','인수인계'];
function json(body,status=200){return new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store, max-age=0','x-content-type-options':'nosniff'}})}
function clean(v,max=1200){return String(v||'').replace(/\s+/g,' ').trim().slice(0,max)}
function decodeHtml(v){return String(v||'').replace(/<!\[CDATA\[([\s\S]*?)\]\]>/g,'$1').replace(/&#(\d+);/g,(_,n)=>String.fromCharCode(Number(n))).replace(/&#x([0-9a-f]+);/gi,(_,n)=>String.fromCharCode(parseInt(n,16))).replace(/&amp;/g,'&').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&quot;/g,'"').replace(/&#39;|&apos;/g,"'").replace(/&nbsp;/g,' ')}
function stripTags(v){return decodeHtml(v).replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim()}
function tag(block,name){const m=String(block||'').match(new RegExp(`<${name}(?:\\s[^>]*)?>([\\s\\S]*?)<\\/${name}>`,'i'));return m?stripTags(m[1]):''}
function sourceHost(url){try{return new URL(url).hostname.replace(/^www\./,'')}catch{return ''}}
function normalizeUrl(url){try{const u=new URL(url);['utm_source','utm_medium','utm_campaign','utm_term','utm_content','gclid','fbclid'].forEach(k=>u.searchParams.delete(k));u.hash='';return u.toString()}catch{return String(url||'')}}
function entityText(v){return String(v||'').normalize('NFKC').toLowerCase().replace(/[\s"'`’‘“”·ㆍ•:;,.!?()[\]{}<>/\\|_+=~^*-]+/g,'')}
function aliases(company){const raw=String(company||'').normalize('NFKC').trim();return [...new Set([raw,raw.replace(/\(주\)|㈜|주식회사|유한회사|재단법인|사단법인/gi,'').trim()])].map(entityText).filter(v=>v.length>=2)}
function relevant(company,item){let u=item.url||'';try{u=decodeURIComponent(u)}catch{}const hay=entityText(`${item.title||''} ${item.snippet||''} ${u}`);return aliases(company).some(a=>hay.includes(a))}
function signalCount(text){const t=String(text||'').toLowerCase();return new Set(SIGNAL_WORDS.filter(w=>t.includes(w.toLowerCase()))).size}
function numberFrom(text,patterns){for(const p of patterns){const m=String(text||'').match(p);if(m){const n=Number(String(m[1]).replace(/,/g,''));if(Number.isFinite(n))return n}}return null}
function decimalFrom(text,patterns){for(const p of patterns){const m=String(text||'').match(p);if(m){const n=Number(m[1]);if(Number.isFinite(n))return n}}return null}
function decodeBase64Url(v){try{let s=String(v||'').replace(/-/g,'+').replace(/_/g,'/');while(s.length%4)s+='=';return atob(s)}catch{return ''}}
function unwrapSearchUrl(v){const raw=decodeHtml(v);try{const u=new URL(raw,'https://www.bing.com');if(/(^|\.)bing\.com$/i.test(u.hostname)){const q=u.searchParams.get('u');if(q){const d=decodeBase64Url(q.startsWith('a1')?q.slice(2):q);if(/^https?:\/\//i.test(d))return d}}const uddg=u.searchParams.get('uddg');if(uddg){const d=decodeURIComponent(uddg);if(/^https?:\/\//i.test(d))return d}return u.href}catch{return raw}}
async function fetchText(url,timeoutMs=6500){const c=new AbortController(),timer=setTimeout(()=>c.abort(),timeoutMs);try{const r=await fetch(url,{headers:{accept:'text/html,application/xhtml+xml,application/rss+xml,application/xml;q=0.9,*/*;q=0.7','user-agent':'Mozilla/5.0 (compatible; YEHAVHA-NEXUS-Reputation/7.0; +https://yehavha.com/)'},signal:c.signal});if(!r.ok)throw new Error(`upstream_${r.status}`);return await r.text()}finally{clearTimeout(timer)}}
function bingRssUrl(q){return `https://www.bing.com/search?format=rss&setlang=ko-KR&cc=KR&q=${encodeURIComponent(q)}`}
function bingHtmlUrl(q){return `https://www.bing.com/search?setlang=ko-KR&cc=KR&count=20&q=${encodeURIComponent(q)}`}
function ddgHtmlUrl(q){return `https://html.duckduckgo.com/html/?kl=kr-kr&q=${encodeURIComponent(q)}`}
function parseRss(xml,query){const out=[];for(const block of String(xml||'').match(/<item\b[\s\S]*?<\/item>/gi)||[]){const title=tag(block,'title'),url=unwrapSearchUrl(tag(block,'link')),snippet=tag(block,'description'),publishedAt=tag(block,'pubDate');if(title&&/^https?:\/\//i.test(url))out.push({title,url,snippet,publishedAt:publishedAt||null,query});if(out.length>=18)break}return out}
function parseBingHtml(html,query){const out=[];for(const block of String(html||'').match(/<li[^>]+class="[^"]*b_algo[^"]*"[\s\S]*?<\/li>/gi)||[]){const a=block.match(/<h2[^>]*>\s*<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/i);if(!a)continue;const p=block.match(/<p[^>]*>([\s\S]*?)<\/p>/i),url=unwrapSearchUrl(a[1]);if(/^https?:\/\//i.test(url))out.push({title:stripTags(a[2]),url,snippet:p?stripTags(p[1]):'',publishedAt:null,query});if(out.length>=18)break}return out}
function parseDdgHtml(html,query){const out=[];for(const block of String(html||'').match(/<div[^>]+class="[^"]*result[^"]*"[\s\S]*?(?=<div[^>]+class="[^"]*result[^"]*"|$)/gi)||[]){const a=block.match(/<a[^>]+class="[^"]*result__a[^"]*"[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/i)||block.match(/<a[^>]+href="([^"]+)"[^>]+class="[^"]*result__a[^"]*"[^>]*>([\s\S]*?)<\/a>/i);if(!a)continue;const s=block.match(/<(?:a|div)[^>]+class="[^"]*result__snippet[^"]*"[^>]*>([\s\S]*?)<\/(?:a|div)>/i),url=unwrapSearchUrl(a[1]);if(/^https?:\/\//i.test(url))out.push({title:stripTags(a[2]),url,snippet:s?stripTags(s[1]):'',publishedAt:null,query});if(out.length>=18)break}return out}
async function runQuery(query){const settled=await Promise.allSettled([fetchText(bingRssUrl(query)).then(x=>parseRss(x,query)),fetchText(bingHtmlUrl(query)).then(x=>parseBingHtml(x,query)),fetchText(ddgHtmlUrl(query)).then(x=>parseDdgHtml(x,query))]);return {items:settled.flatMap(r=>r.status==='fulfilled'?r.value:[]),successes:settled.filter(r=>r.status==='fulfilled').length,failures:settled.filter(r=>r.status==='rejected').length}}
function queryPlan(company){const q=`"${company}"`;return [`${q} 재직자 퇴사 면접 후기`,`${q} 기업리뷰 워라밸 조직문화 급여 복지`,`site:jobplanet.co.kr/companies ${q}`,`site:teamblind.com ${q}`,`site:saramin.co.kr ${q} 기업리뷰`,`site:jobkorea.co.kr ${q} 기업리뷰 면접후기`,`site:blog.naver.com ${q} 회사 후기`,`site:cafe.naver.com ${q} 면접 후기`]}
function dedupe(items){const seen=new Set(),out=[];for(const item of items){const key=normalizeUrl(item.url);if(!key||seen.has(key))continue;seen.add(key);out.push({...item,url:key})}return out}
function platformName(host){const h=String(host||'').toLowerCase();if(h.includes('jobplanet'))return '잡플래닛';if(h.includes('teamblind')||/(^|\.)blind\./.test(h))return '블라인드';if(h.includes('saramin'))return '사람인';if(h.includes('jobkorea'))return '잡코리아';if(h.includes('incruit'))return '인크루트';if(h.includes('blog.naver'))return '네이버 블로그';if(h.includes('cafe.naver'))return '네이버 카페';if(h.includes('tistory'))return '티스토리';return host||'공개 웹'}
function isConcreteOpinion(company,item){if(!relevant(company,item))return false;const title=clean(item.title,280),snippet=clean(item.snippet,1200);if(snippet.length<28||!EXPERIENCE_TERMS.test(snippet)||signalCount(snippet)<2)return false;const actual=ACTUAL_VOICE.test(snippet),reviewSpecific=REVIEW_URL.test(item.url||'')||/(후기|리뷰|재직자|전직자|퇴사|면접)/i.test(title);if(!actual&&!reviewSpecific)return false;if(GENERIC_ONLY.test(title)&&!actual)return false;return true}
function extractExcerpt(item){const snippet=clean(item.snippet,700),parts=snippet.split(/(?<=[.!?。])\s+|\s*[·•]\s*/).map(s=>clean(s,260)).filter(Boolean),selected=parts.filter(p=>EXPERIENCE_TERMS.test(p)&&(ACTUAL_VOICE.test(p)||signalCount(p)>=2)&&!GENERIC_ONLY.test(p)).slice(0,3);return clean(selected.length?selected.join(' / '):snippet,520)}
function topicsFor(text){const t=String(text||'').toLowerCase(),found=Object.entries(TOPICS).filter(([,words])=>words.some(w=>t.includes(w.toLowerCase()))).map(([name])=>name);return found.length?found:['기타 근무경험']}
function buildOpinions(company,candidates){const seen=new Set(),out=[];for(const item of candidates.filter(i=>isConcreteOpinion(company,i))){const excerpt=extractExcerpt(item);if(excerpt.length<24)continue;const key=`${normalizeUrl(item.url)}|${entityText(excerpt).slice(0,160)}`;if(seen.has(key))continue;seen.add(key);const host=sourceHost(item.url);out.push({...item,host,platform:platformName(host),excerpt,topics:topicsFor(excerpt)});if(out.length>=40)break}return out.map((item,i)=>({id:`R${String(i+1).padStart(2,'0')}`,...item}))}
function buildThemes(opinions){const m=new Map();for(const o of opinions)for(const topic of o.topics){if(!m.has(topic))m.set(topic,[]);m.get(topic).push(o.id)}return [...m.entries()].map(([topic,ids])=>({topic,count:ids.length,opinionIds:ids})).sort((a,b)=>b.count-a.count||a.topic.localeCompare(b.topic,'ko')).slice(0,8)}
function buildPlatforms(opinions){const m=new Map();for(const o of opinions){if(!m.has(o.platform))m.set(o.platform,{name:o.platform,count:0,hosts:new Set()});const x=m.get(o.platform);x.count++;if(o.host)x.hosts.add(o.host)}return [...m.values()].map(x=>({name:x.name,count:x.count,hosts:[...x.hosts]})).sort((a,b)=>b.count-a.count)}
function buildActions(themes){return themes.filter(t=>ACTIONS[t.topic]).slice(0,6).map(t=>({title:t.topic,text:ACTIONS[t.topic],refs:t.opinionIds.slice(0,5)}))}
function parsePlatformSignals(candidates){const items=candidates.filter(x=>/jobplanet\.co\.kr$/i.test(sourceHost(x.url)));if(!items.length)return [];const text=items.map(x=>`${x.title} ${x.snippet}`).join(' '),reviewCount=numberFrom(text,[/전체 리뷰 통계\s*\(?([\d,]+)명\)?/i,/기업리뷰\s*([\d,]+)건/i,/리뷰\s*([\d,]+)건/i]),interviewCount=numberFrom(text,[/면접후기\s*([\d,]+)건/i]),rating=decimalFrom(text,[/([0-5](?:\.\d+)?)\s*리뷰평점/i,/리뷰평점\s*([0-5](?:\.\d+)?)/i]);if(reviewCount===null&&interviewCount===null&&rating===null)return [];return [{platform:'잡플래닛',host:'jobplanet.co.kr',url:items[0].url,reviewCount,interviewCount,rating,note:'공개 검색에 노출된 잡플래닛 집계 정보입니다.'}]}
function summary(company,opinions,themes,signals){const signal=signals.length?signals.map(x=>[x.platform,Number.isFinite(Number(x.reviewCount))?`리뷰 ${Number(x.reviewCount).toLocaleString('ko-KR')}건`:'',Number.isFinite(Number(x.rating))?`평점 ${Number(x.rating).toFixed(1)}/5`:''].filter(Boolean).join(' · ')).join(' / '):'';if(!opinions.length)return `${company}의 평판 후기 본문은 검색 색인·로그인 제한 때문에 자동으로 직접 인용 가능한 범위가 제한될 수 있습니다.${signal?` ${signal}이 확인됩니다.`:''} 이는 회사에 후기가 없다는 뜻이 아니며 회사 개요·채용·연봉·뉴스·공식자료는 별도 공개정보 분석에서 함께 제공합니다.`;const top=themes.slice(0,4).map(t=>`${t.topic} ${t.count}건`).join(', ');return `${company}의 공개 검색에서 실제 경험 문장 ${opinions.length}건을 확인했습니다.${signal?` ${signal}.`:''}${top?` 반복 주제는 ${top}입니다.`:''} 한두 개 후기보다 반복성·최신성·직군 차이를 중심으로 해석합니다.`}
function searchLinks(company){const q=encodeURIComponent(company),quoted=encodeURIComponent(`"${company}"`);return [{label:'Google 검색',url:`https://www.google.com/search?q=${quoted}`},{label:'네이버 검색',url:`https://search.naver.com/search.naver?where=nexearch&query=${q}`},{label:'잡플래닛 검색',url:`https://www.jobplanet.co.kr/search?query=${q}`},{label:'사람인 검색',url:`https://www.saramin.co.kr/zf_user/search?searchword=${q}`},{label:'잡코리아 검색',url:`https://www.jobkorea.co.kr/Search/?stext=${q}`},{label:'블라인드 검색',url:`https://www.google.com/search?q=site%3Ateamblind.com+${quoted}`}]}
async function collect(company){const results=await Promise.all(queryPlan(company).map(runQuery)),raw=dedupe(results.flatMap(r=>r.items)),matched=raw.filter(x=>relevant(company,x)),platformSignals=parsePlatformSignals(matched),opinions=buildOpinions(company,matched);return {raw,matched,platformSignals,opinions,providerSuccesses:results.reduce((n,r)=>n+r.successes,0),providerFailures:results.reduce((n,r)=>n+r.failures,0)}}
export async function onRequestPost({request}){let body;try{body=await request.json()}catch{return json({ok:false,error:'invalid_json'},400)}const company=clean(body?.company||body?.target,120);if(company.length<2)return json({ok:false,error:'company_too_short'},400);const c=await collect(company),themes=buildThemes(c.opinions),platforms=buildPlatforms(c.opinions),actions=buildActions(themes);return json({ok:true,schema:'nexus-company-reputation-v3',company,generatedAt:new Date().toISOString(),summary:summary(company,c.opinions,themes,c.platformSignals),metrics:{rawCandidates:c.raw.length,companyMatched:c.matched.length,acceptedOpinions:c.opinions.length,platforms:platforms.length,themes:themes.length,providerSuccesses:c.providerSuccesses,providerFailures:c.providerFailures},platformSignals:c.platformSignals,themes,platforms,actions,opinions:c.opinions.map(o=>({id:o.id,title:o.title,url:o.url,excerpt:o.excerpt,publishedAt:o.publishedAt,host:o.host,platform:o.platform,topics:o.topics})),searchLinks:searchLinks(company)})}
export async function onRequestGet(){return json({ok:true,service:'NEXUS 구직자 회사 평판 분석',version:'7.0',schema:'nexus-company-reputation-v3',scope:'public-company-reputation-intelligence'})}