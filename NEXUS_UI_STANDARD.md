# YEHAVHA Nexus UI & Document Standard

Version: 1.1  
Updated: 2026-08-09

## 1. 적용 원칙

YEHAVHA Nexus와 직접 운영하는 하위 프로젝트는 서로 다른 기능을 유지하되 동일한 포털 계열이라는 시각적·구조적 일관성을 유지한다. 외부 서비스(U-Paper, YouTube 등)의 서비스 내부 화면은 이 표준의 직접 적용 대상에서 제외하고 Nexus 카드와 이동 규칙만 적용한다.

## 2. 공통 시각 언어

Nexus 실제 운영 화면을 기준 원본으로 한다.

- 페이지 기본 배경: `#071225`
- Deep Navy: `#071627`
- Navy: `#081A30`
- Panel: `rgba(8,18,38,.78)` 또는 불투명 대체색 `#0B1D33`
- Panel 강조: `#0D2948`
- 기본 글자: `#F2F6FF`
- 보조 글자: `#B8C9DF`
- 강조색: `#7AB8FF`
- 구분선: `rgba(255,255,255,.10)`
- 기본 서체: Pretendard → Noto Sans KR → Apple SD Gothic Neo → system sans-serif
- 연구문서에서 장식용 serif 서체를 별도 사용하지 않는다.
- 이모지 아이콘을 구조 표시에 사용하지 않는다. 상위 카테고리는 `01–05`, 문서 항목은 `01–99`의 두 자리 숫자를 사용한다.

### 배경 통일 원칙

- 직접 운영 프로젝트의 `html`, `body` 기본 배경은 `#071225`로 통일한다.
- 페이지 전체를 흰색 또는 프로젝트별 고유색으로 덮지 않는다.
- 큰 섹션은 기본적으로 투명 배경을 사용하고 Nexus 배경이 이어지게 한다.
- 카드·패널·검색영역·학습영역은 `rgba(8,18,38,.78)` 또는 `#0B1D33`을 사용한다.
- 프로젝트별 배경사진은 핵심 기능상 필요한 경우가 아니면 사용하지 않는다. 기존 배경사진이 있더라도 Nexus 표준 적용 시 기본 배경색이 우선한다.
- 외부 서비스 화면은 적용하지 않는다.

## 3. 너비와 여백

- 포털·연구 메인 최대 폭: 1160px
- 연구 상세문서 본문 최대 폭: 850px
- 모바일 좌우 최소 여백: 10px
- 기본 간격 단위: 4 / 8 / 12 / 16 / 24 / 32 / 44 / 64px
- 같은 층위 카드의 내부 여백·모서리·버튼 높이를 동일하게 유지한다.
- 의미 없는 빈 공간을 만들기 위한 고정 높이는 사용하지 않는다. 기능상 필요한 학습카드 높이는 예외로 한다.

## 4. 글자 규격

직접 운영 프로젝트는 아래 범위에서 통일한다.

- 기본 본문: `15px`, line-height `1.72–1.78`
- 모바일 기본 본문: `15px`
- H1: `clamp(34px, 4.8vw, 52px)`
- H2: `clamp(22px, 3vw, 28px)`
- H3·카드 제목: `18px`
- 연구 상세문서 항목 제목: `16–17px`
- 카드 요약·설명: `14–15px`
- 보조 문구: `12–13px`
- 메타·eyebrow·태그: `10.5–11.5px`
- Footer: `11–12px`
- 제목 끝에 불필요한 마침표를 붙이지 않는다.
- 특정 프로젝트에서 `!important`로 16px 이상 본문이나 30–40px H2를 강제하여 다른 프로젝트보다 크게 보이게 하지 않는다.

## 5. 연구문서 표준

AI 법률연구소, 법리·판례 연구, AI 법·기술 선제연구 아카이브, 법철학·기본권 연구는 동일 연구문서 규칙을 따른다.

1. 문서 헤더: 연구분류 → 제목 → 요약 → 메타정보
2. 목차: 열린 상세문서에 자동 생성하고 각 항목으로 직접 이동할 수 있게 한다.
3. 번호: 실제 표시되는 항목만 `01, 02, 03...` 순서로 연속 번호를 부여한다.
4. 본문 폭: 최대 850px
5. 출처: 문서 후반부에서 동일 버튼·링크 규격으로 표시한다.
6. 하단: 프로젝트명, `Copyright © 이명훈 2026. All rights reserved.`, 문의 이메일, AI 활용 안내, `맨 위로`, `창 닫기`를 공통 배치한다.
7. 스크롤: 상세문서에서는 스크롤 컨테이너를 하나만 사용한다.
8. 다이얼로그: 폭 최대 960px, 높이 최대 92dvh, 닫기 버튼은 우측 상단 절대배치한다.
9. `float + sticky`, 음수 margin으로 닫기 버튼을 배치하지 않는다.
10. 다이얼로그 오픈 시 배경 페이지 스크롤을 잠그고 `scrollbar-gutter: stable`을 사용한다.

### 학술문헌 표기

- 학자명은 영문 원이름을 먼저 표기하고 한글명은 괄호 또는 보조행으로 병기한다.
- 저작명은 영문 원저명을 먼저 표기하고 한글 번역명은 괄호 또는 보조행으로 병기한다.
- 각주·참고문헌 데이터의 원문 서지정보는 번역명으로 대체하지 않는다.

## 6. 목록·카드 표준

- 같은 층위 카드는 동일 radius와 padding을 사용한다.
- hover 시 카드가 위아래로 이동하는 transform 효과를 사용하지 않는다.
- 카드 제목·요약·메타·행동버튼의 순서를 고정한다.
- 행동버튼 최소 높이는 40px로 한다.
- 상태, 업데이트일, 콘텐츠 수는 메타정보 층위에서만 표시한다.
- 카드 배경은 Nexus Panel 색을 기본으로 하고 카드별 흰색·회색·유채색 배경을 혼용하지 않는다.

## 7. 이동 규칙

- Nexus 내부에서 직접 운영하는 프로젝트: 현재 탭에서 이동한다.
- 각 직접 운영 프로젝트 상단: `← YEHAVHA NEXUS`를 동일 규격으로 제공한다.
- U-Paper, YouTube 등 외부 서비스: 새 탭에서 연다.
- 외부 이동은 `↗`, 내부 이동은 `→`, 상위 포털 복귀는 `←`를 사용한다.
- `맨 위로` 표기는 `맨 위로 ↑`로 통일한다.

## 8. 저작권·문의 표준

직접 운영하는 모든 공개 페이지와 상세문서에는 다음 정보를 포함한다.

`Copyright © 이명훈 2026. All rights reserved.`  
`문의 kimbrighth@gmail.com`

저작권은 페이지 최하단뿐 아니라 별도의 상세문서·모달·연구노트가 독립적으로 열리는 경우에도 해당 문서 하단에 표시한다.

## 9. 웹앱·교육 프로젝트

웹앱과 강좌는 고유 기능·콘텐츠 구조를 유지하되 다음 외곽 요소와 시각 토큰은 공통화한다.

- Nexus 배경색
- 기본 서체와 글자 위계
- Nexus 복귀 링크
- 카드·패널 기본색
- 안정적 scrollbar
- 버튼 최소 높이
- 하단 저작권·문의·AI 활용 안내
- 외곽 여백과 모바일 안전영역

## 10. 유지보수

새 프로젝트를 Nexus에 추가할 때는 먼저 이 표준을 적용한 뒤 공개한다. 기존 프로젝트의 개별 CSS가 표준과 충돌하면 프로젝트 고유 콘텐츠 표현보다 Nexus 공통 내비게이션·배경·타이포그래피·문서 규격·저작권 규칙을 우선한다.
