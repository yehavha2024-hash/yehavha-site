name: AI Literature Audit

on:
  push:
    paths:
      - 'legal-knowledge/ai-literature/**'
      - 'scripts/audit-ai-literature.mjs'
      - '.github/workflows/ai-literature-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Audit AI literature archive
        run: node scripts/audit-ai-literature.mjs
