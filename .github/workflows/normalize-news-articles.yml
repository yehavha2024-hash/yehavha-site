name: Normalize YEHAVHA NEWS article shell

on:
  push:
    branches: [main]
    paths:
      - 'nexus/news/articles/**'
      - 'nexus/scripts/normalize-news-articles.mjs'
      - 'scripts/ci/safe-content-commit.sh'
      - '.github/workflows/normalize-news-articles.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: yehavha-news-shell-normalize
  cancel-in-progress: true

jobs:
  normalize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Normalize article shell
        run: node nexus/scripts/normalize-news-articles.mjs

      - name: Publish shell repairs when needed
        shell: bash
        run: bash scripts/ci/safe-content-commit.sh "Normalize YEHAVHA NEWS article shell" nexus/news/articles
