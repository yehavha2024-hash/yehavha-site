name: Refresh Nexus project status

on:
  push:
    branches: [main]
    paths:
      - 'nexus/**'
      - '!nexus/project-status.json'
      - '!nexus/sitemap.xml'
      - 'nexus.project.json'
      - 'three-minute-break/**'
      - 'toeic-human-100/**'
      - 'legal-knowledge/**'
      - 'ai-law-tech-foresight/**'
      - 'legal-philosophy/**'
      - '.github/workflows/refresh-nexus-status.yml'
  schedule:
    - cron: '15 15 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: nexus-status-refresh
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Refresh Nexus project status
        run: node nexus/scripts/update-status.mjs

      - name: Refresh Nexus sitemap
        run: node nexus/scripts/update-sitemap.mjs

      - name: Commit Nexus generated metadata when changed
        shell: bash
        run: |
          if git diff --quiet -- nexus/project-status.json nexus/sitemap.xml; then
            echo "Nexus generated metadata is already current."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add nexus/project-status.json nexus/sitemap.xml
          git commit -m "Refresh Nexus generated metadata"
          git push
