name: Install business development UI

on:
  push:
    branches: [main]
    paths:
      - 'scripts/install-business-development-ui.py'
      - 'nexus/local-government-planning/business-development-render.js'
      - '.github/workflows/install-business-development-ui.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Install business development renderer
        run: python scripts/install-business-development-ui.py
      - name: Commit index change
        shell: bash
        run: |
          if git diff --quiet -- nexus/local-government-planning/index.html; then
            echo 'Already installed.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add nexus/local-government-planning/index.html
          git commit -m 'Connect public business development manual'
          git push
